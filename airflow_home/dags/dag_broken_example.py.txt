# ============================================================================
# THIS FILE IS INTENTIONALLY NAMED .py.txt SO AIRFLOW/CI IGNORES IT BY DEFAULT.
#
# HOW TO USE:
# Rename this file to dag_broken_example.py and push it in a test PR to see
# the GitHub Action correctly FAIL the validation check. Then rename it back
# (or delete it) once you've confirmed the workflow catches errors properly.
# ============================================================================
"""
dag_broken_example.py
Deliberately broken DAG (missing import + syntax issue) to prove
that dag-validation.yml correctly fails the PR check.
"""
from datetime import datetime, timedelta

from airflow import DAG
from airflow.operators.python import PythonOperator


def broken_task_function()  # <-- missing colon: this is a SyntaxError
    print("This will never run")


default_args = {
    "owner": "data_platform_team",
    "retries": 1,
}

with DAG(
    dag_id="dag_broken_example",
    default_args=default_args,
    schedule_interval="@daily",
    start_date=datetime(2024, 1, 1),
    catchup=False,
) as dag:

    broken_task = PythonOperator(
        task_id="broken_task",
        python_callable=broken_task_function,
    )
